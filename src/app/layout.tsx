import type { ReactNode } from "react";
import { AppProviders } from "./providers"; // Import the new client provider
import "./globals.css"; 

export const metadata = {
  title: 'Next.js',
  description: 'Generated by Next.js',
}

// The root layout is now a clean Server Component.
export default ({ children }: { children: ReactNode }) => (
  <html lang="en">
    <body>
      {/* It just renders the AppProviders, which handles all
          the Fogo logic and client-side layout */}
      <AppProviders>
        {children}
      </AppProviders>
    </body>
  </html>
);

