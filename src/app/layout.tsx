import type { ReactNode } from "react";
import { AppProviders } from "./providers"; // <-- 1. Import new provider component
import "./globals.css"; 

export const metadata = {
  title: 'Next.js',
  description: 'Generated by Next.js',
}

export default ({ children }: { children: ReactNode }) => (
  <html lang="en">
    <body>
      {/* 2. Render the new provider. All Fogo logic is now in that file. */}
      <AppProviders>
        {children}
      </AppProviders>
    </body>
  </html>
);

